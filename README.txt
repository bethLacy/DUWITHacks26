To install:
Run "npm install" in the SERVER folder through cmd prompt 

To start server:
Run "npm start" in the SERVER folder though cmd prompt

To open webpage:
The site should be accessed through the index.html page in the CLIENT folder